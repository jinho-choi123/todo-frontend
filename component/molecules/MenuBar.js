import { AppBar, Box } from "@mui/material"

const MenuBar = () => {
    return (
        <Box sx={{flexGrow: 1}}>
            <AppBar position="static">
                hello
            </AppBar>
        </Box>
    )
}

export default MenuBar